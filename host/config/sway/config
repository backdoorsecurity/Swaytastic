############################################ [SWAY CONFIG] ###########################################

############################################# [VARIABLES] ##########################################

include $HOME/.config/sway/variables

###############################################################################################


######################################## [KEYBINDINGS] ########################################
	
	bindsym $mod+a workspace $ws_brave
	bindsym $mod+s workspace $ws_term
	bindsym $mod+d workspace $ws_term2
        bindsym $mod+f workspace $ws_term3
	bindsym $mod+g workspace $ws_virt
	
	bindsym $mod+j focus left
	bindsym $mod+Down focus down
	bindsym $mod+Up focus up
	bindsym $mod+k focus right
	
	bindsym $mod+Shift+r reload
	
	bindsym $mod+x kill
        bindsym $mod+q workspace $ws_brave, exec --no-startup-id brave-origin-nightly
        bindsym $mod+w exec --no-startup-id foot
        bindsym $mod+e exec --no-startup-id foot
        bindsym $mod+r exec --no-startup-id foot
        bindsym $mod+t workspace $ws_virt, exec --no-startup-id virt-manager
	bindsym $mod+i fullscreen
	bindsym $mod+Escape exec --no-startup-id poweroff
	bindsym $mod+Shift+d exec pcmanfm-qt --desktop-pref --profile=sway

#	bindsym $mod+0 layout stacking
#	bindsym $mod+9 layout tabbed
#	bindsym $mod+8 layout toggle split
#	mode "resize" {
#	up/down height left/right width
#	bindsym Left resize shrink width 10px
#	You can "split" the current object of your focus with
#	$mod+b or $mod+v, for horizontal and vertical splits
#	respectively.
#	bindsym $mod+0 splith
#	bindsym $mod+9 splitv
#	Toggle the current focus between tiling and floating mode
#	bindsym $mod+8 floating toggle

	bindsym --locked XF86AudioMute exec pactl set-sink-mute \@DEFAULT_SINK@ toggle
	bindsym --locked XF86AudioLowerVolume exec pactl set-sink-volume \@DEFAULT_SINK@ -5%
	bindsym --locked XF86AudioRaiseVolume exec pactl set-sink-volume \@DEFAULT_SINK@ +5%
	bindsym --locked XF86AudioMicMute exec pactl set-source-mute \@DEFAULT_SOURCE@ toggle
	bindsym --locked XF86MonBrightnessDown exec brightnessctl set 5%-
	bindsym --locked XF86MonBrightnessUp exec brightnessctl set 5%+
	bindsym Print exec grim

############################################################################################################

######################################## [INPUTS/OUTPUTS] ##################################################

#	[DISPLAY]

	output * scale 1.5

#	[TOUCHPAD]
	input * { 
	dwt enabled
	tap enable
	accel_profile adaptive
        pointer_accel 1 drag enabled
        #middle_emulation enabled
}
#	[KEYBOARD]
        input * { 
	repeat_delay 250
	repeat_rate 60
}

#########################################################################################################

########################################## [STYLING] ####################################################

# [Status Bar] swaybar cannot outline the whole bar; waybar CSS can.
bar {
    position top
    swaybar_command waybar
}

# pin apps onto the static workspaces (terminals stay unassigned)
assign [app_id="brave-origin-nightly"] $ws_brave
assign [app_id="brave-browser"] $ws_brave
assign [class="Brave-browser"] $ws_brave
assign [app_id="org.virt-manager.virt-manager"] $ws_virt
assign [class="Virt-manager"] $ws_virt
assign [class="virt-manager"] $ws_virt

# [WINDOW SIZING]
#
titlebar_border_thickness 0
default_border pixel 0
hide_edge_borders none
gaps left 0
gaps right 0
gaps bottom 0
gaps top 0
#for_window [app_id="foot"] border pixel 0
# Qt 6.8 on sway 1.10 often maps QMenus as xdg_toplevel, not xdg_popup.
# Tiling those windows makes right-click look like a no-op. Layer-shell desktop is not a managed window.
for_window [app_id="pcmanfm-qt"] floating enable
#default_floating_border pixel 8 8
#titlebar_padding 3 3
#gaps inner 2
#title_align center

exec_always brightnessctl set 70%
exec_always pgrep -x wlsunset || wlsunset -t 3200
# desktop icons (layer-shell). exec, not exec_always, so reload does not duplicate.
exec pcmanfm-qt --desktop --profile=sway
font monospace 16
include /etc/sway/config-vars.d/*
include /etc/sway/config.d/*
