/* Local Swaytastic/qt6ct chrome: accent outline boxes, extra item padding. */

QMenu {
    background: #000000;
    color: #99e01c;
    border: 2px solid #de1062;
    padding: 6px;
    margin: 0px;
}

QMenu::item {
    background: transparent;
    color: #99e01c;
    padding: 6px 28px 6px 12px;
    margin: 1px 0px;
}

QMenu::item:selected {
    background: #de1062;
    color: #99e01c;
}

QMenu::item:disabled {
    color: #4a7a0e;
}

QMenu::separator {
    height: 2px;
    background: #de1062;
    margin: 6px 8px;
}

QMenu::right-arrow {
    width: 10px;
    height: 10px;
}

QFrame, QGroupBox, QTabWidget::pane {
    border: 2px solid #de1062;
}

QGroupBox {
    margin-top: 10px;
    padding: 10px 8px 8px 8px;
}

QLineEdit, QTextEdit, QPlainTextEdit, QSpinBox, QComboBox, QAbstractSpinBox {
    background: #000000;
    color: #99e01c;
    border: 2px solid #de1062;
    padding: 4px 8px;
    selection-background-color: #de1062;
    selection-color: #99e01c;
}

QComboBox::drop-down {
    border-left: 2px solid #de1062;
    width: 22px;
    background: #de1062;
}

/* Desktop is a fullscreen layer-shell surface (exclusive zone -1), so a
   QFrame border here becomes a screen outline and also wraps waybar.
   Namespaced class is PCManFM::DesktopWindow → PCManFM--DesktopWindow. */
PCManFM--DesktopWindow,
PCManFM--DesktopWindow QFrame,
PCManFM--DesktopWindow QListView,
PCManFM--DesktopWindow QAbstractScrollArea,
PCManFM--DesktopWindow QAbstractScrollArea::viewport,
DesktopWindow,
DesktopWindow QFrame,
DesktopWindow QListView,
DesktopWindow QAbstractScrollArea,
DesktopWindow QAbstractScrollArea::viewport {
    border: 0px solid transparent;
    outline: 0px;
    qproperty-frameShape: 0;
    qproperty-lineWidth: 0;
    qproperty-midLineWidth: 0;
}

/* Folder windows only. A global QListView fill pins pcmanfm-qt's desktop
   BgColor/FgColor (Preferences → Desktop) back to black/green. */
QMainWindow QTreeView, QDialog QTreeView,
QMainWindow QListView, QDialog QListView,
QMainWindow QTableView, QDialog QTableView,
QMainWindow QColumnView, QDialog QColumnView {
    background: #000000;
    color: #99e01c;
    border: 2px solid #de1062;
    padding: 4px;
    outline: 0;
}

QTreeView::item, QListView::item, QTableView::item {
    padding: 5px 8px;
    min-height: 22px;
}

QTreeView::item:selected, QListView::item:selected, QTableView::item:selected {
    background: #de1062;
    color: #99e01c;
}

QHeaderView::section {
    background: #000000;
    color: #99e01c;
    border: 1px solid #de1062;
    padding: 6px 10px;
}

QSplitter::handle {
    background: #de1062;
}

QSplitter::handle:horizontal {
    width: 3px;
}

QSplitter::handle:vertical {
    height: 3px;
}

QScrollBar:vertical {
    background: #000000;
    width: 12px;
    border: 2px solid #de1062;
    margin: 0;
}

QScrollBar:horizontal {
    background: #000000;
    height: 12px;
    border: 2px solid #de1062;
    margin: 0;
}

QScrollBar::handle:vertical, QScrollBar::handle:horizontal {
    background: #de1062;
    min-height: 24px;
    min-width: 24px;
}

QScrollBar::add-line, QScrollBar::sub-line {
    width: 0;
    height: 0;
}

QPushButton {
    background: #000000;
    color: #99e01c;
    border: 2px solid #de1062;
    padding: 5px 14px;
}

QPushButton:hover, QPushButton:focus {
    background: #000010;
}

QToolTip {
    background: #000000;
    color: #99e01c;
    border: 2px solid #de1062;
    padding: 4px 8px;
}

QStatusBar {
    border-top: 2px solid #de1062;
}

/* Fusion derives checkbox/radio outlines from Window.darker(). Window is
   #000000, so the indicators are black-on-black. Draw them explicitly. */
QCheckBox, QRadioButton {
    color: #99e01c;
    spacing: 8px;
}

QCheckBox::indicator, QGroupBox::indicator {
    width: 14px;
    height: 14px;
    border: 2px solid #de1062;
    background: #000000;
}

QCheckBox::indicator:checked, QGroupBox::indicator:checked {
    background: #99e01c;
}

QCheckBox::indicator:disabled, QGroupBox::indicator:disabled {
    border-color: #6f0831;
    background: #100909;
}

QCheckBox::indicator:checked:disabled, QGroupBox::indicator:checked:disabled {
    background: #4a7a0e;
}

QRadioButton::indicator {
    width: 14px;
    height: 14px;
    border: 2px solid #de1062;
    border-radius: 8px;
    background: #000000;
}

QRadioButton::indicator:checked {
    background: #99e01c;
}

QRadioButton::indicator:disabled {
    border-color: #6f0831;
    background: #100909;
}

QRadioButton::indicator:checked:disabled {
    background: #4a7a0e;
}

QMenu::indicator {
    width: 12px;
    height: 12px;
    border: 2px solid #de1062;
    background: #000000;
}

QMenu::indicator:checked {
    background: #99e01c;
}

QAbstractSpinBox::up-button, QAbstractSpinBox::down-button {
    width: 16px;
    background: #de1062;
    border: none;
}
